interface:
  display_name: "Record"
  short_description: "Keep a local continuity record across long sessions"
  default_prompt: "Use $record to start a temporary local conversation record and keep it current through later turns."
policy:
  allow_implicit_invocation: false
