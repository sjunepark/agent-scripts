interface:
  display_name: "Interview"
  short_description: "Resolve open items from the conversation or prompt"
  default_prompt: "Use $interview to clear the important open items from our preceding conversation and anything I name in this prompt, confirm a compact decision readback, then act."
policy:
  allow_implicit_invocation: false
