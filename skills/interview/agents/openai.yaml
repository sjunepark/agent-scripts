interface:
  display_name: "Interview"
  short_description: "Brief options and resolve open decisions"
  default_prompt: "Use $interview to explain the open decisions, ask where my judgment is needed, and carry settled choices into the authorized task."
policy:
  allow_implicit_invocation: false
