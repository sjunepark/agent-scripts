interface:
  display_name: "Interview"
  short_description: "Brief options and resolve open decisions"
  default_prompt: "Use $interview to brief me on every important open item and its credible options, ask where my judgment is needed, and give one decision readback. Carry settled choices into the authorized task."
policy:
  allow_implicit_invocation: false
