interface:
  display_name: "Interview"
  short_description: "Brief and resolve consequential open decisions"
  default_prompt: "Use $interview to brief me on and resolve the important open items from our preceding conversation and this prompt, confirm a compact decision readback, then act."
policy:
  allow_implicit_invocation: false
