interface:
  display_name: "Progress Run"
  short_description: "Run the next plan slice with updates"
  default_prompt: "Use $progress-run to continue the next clear slice from docs/PLAN.md and update progress as you go."
