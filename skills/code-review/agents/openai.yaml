interface:
  display_name: "Code Review"
  short_description: "Review code with implementation, design, and diet lenses"
  default_prompt: "Use $code-review to review the current implementation, apply obvious safe Bucket I fixes, validate, and add architecture/design or diet lenses when relevant."
