interface:
  display_name: "Clarify"
  short_description: "Clarify the current prompt's intent and unknowns"
  default_prompt: "Use $clarify to clarify the request in this prompt, recover its consequential unknowns, confirm a compact readback, then proceed."
policy:
  allow_implicit_invocation: false
