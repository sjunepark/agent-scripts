interface:
  display_name: "Delegate"
  short_description: "Guide a Luna implementer and review its work"
  default_prompt: "Use $delegate to orchestrate this implementation through GPT-5.6 Luna and review the completed changes."
policy:
  allow_implicit_invocation: false
