Deletion commands succeeded for local and remote `feature/finished` at tip `E1`, but cleanup remains unverified.

- Local absence: unknown because follow-up inspection failed with a process error.
- Remote absence: unknown because the server query timed out.

Neither deletion can be reported as independently verified.
