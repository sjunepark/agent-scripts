interface:
  display_name: "Post-Implementation Review"
  short_description: "Review changes and apply obvious fixes"
  default_prompt: "Use $post-implementation-review to review my current implementation and apply obvious safe fixes."
