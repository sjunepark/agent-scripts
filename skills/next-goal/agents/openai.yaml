interface:
  display_name: "Next Goal"
  short_description: "Choose a goal with PR and no-PR prompts"
  default_prompt: "Use $next-goal to inspect this repository and return two concise /goal prompts, one with PR delivery and one without PR creation."
policy:
  allow_implicit_invocation: false
