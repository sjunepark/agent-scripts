interface:
  display_name: "Next Goal"
  short_description: "Choose a substantial implementation goal"
  default_prompt: "Use $next-goal to inspect this repository and return a concise prompt for a substantial /goal."
policy:
  allow_implicit_invocation: false
