interface:
  display_name: "Handoff"
  short_description: "Create durable thread handoffs"
  default_prompt: "Use $handoff to write a durable Markdown handoff plan and move this work to a fresh Codex thread."

policy:
  allow_implicit_invocation: false
