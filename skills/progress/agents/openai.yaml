interface:
  display_name: "Progress"
  short_description: "Manage and brief queues and bounded goal state"
  default_prompt: "Use $progress to brief or reconcile project work, or to initialize, recover, and enforce a durable goal-scoped execution contract."

policy:
  allow_implicit_invocation: false
