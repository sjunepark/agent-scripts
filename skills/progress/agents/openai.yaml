interface:
  display_name: "Progress"
  short_description: "Manage queues and recover bounded goal state"
  default_prompt: "Use $progress to reconcile project work or initialize, recover, and enforce a durable goal-scoped execution contract."

policy:
  allow_implicit_invocation: false
