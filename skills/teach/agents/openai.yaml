interface:
  display_name: "Teach"
  short_description: "Teach existing code through design and contracts"
  default_prompt: "Use $teach to explain this existing codebase, subsystem, architecture, or feature flow through its responsibilities, contracts, data flow, invariants, and tradeoffs."
