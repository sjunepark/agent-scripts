interface:
  display_name: "Sync"
  short_description: "Publish and install selected repo skills"
  default_prompt: "Use $sync to commit and push intended skill changes, then install selected published skills from the remote skills/ source with explicit --skill <name> and the default --copy -g -a '*' -y all-agent target."
