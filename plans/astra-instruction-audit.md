# Instruction audit

Status: source revisions, review, publication, and project reconciliation complete.

Scope: apply current Astra guidance to shared instructions and the published
catalog. Consolidate overlapping rules, retain task steering and the Git
workflow fixes, and preserve deliberate authority and activation boundaries.
Coverage, validation, and limits are recorded in the
[evaluation](../skills/develop-skills/evals/evaluation-2026-09-07.md).

Published `a4d03b0` to `main` and verified its skill trees before reconciliation.
The committed `dev` and `go` profiles applied successfully; the final project
plan contains only unchanged placements. The fixed global baseline also has
no drift; protected vendor, backup, cache, and runtime locations remain excluded.

Retain project quarantine `0a018db15308467a3915c5f7a02c5d84` through a normal
work cycle. Restore is a separate operation requiring its exact identifier.

The personal Codex, Claude, and Pi instruction symlinks resolve to the edited
sources. Active-session reloading remains unverified. No global mutation occurred.
