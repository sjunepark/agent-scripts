# Clean

Run the helper's `clean` operation once for the current project and consume its TOON result.

Report the exact `removed` count. Also state that deletion cannot erase native Codex transcripts or memo content already loaded into the current model context.

Do not delete anything else, recurse, follow symlinks, or remove the memo directory.
